/**
 * Next.js config.
 *
 * Nothing special needed here: product images are rendered with plain
 * <img> tags (see components/ProductCard.tsx) rather than next/image, so
 * there's no remote image domain allow-list to maintain.
 */
const nextConfig = {};

export default nextConfig;
